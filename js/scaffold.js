'use strict'

//Create an array of times

//create a store constructor function
//params: name, mincph, maxcph, avg
//Call the calculate function
//Cache the store so we can refer to it later
//The stores total comes comes/stores where?

//Calculate Sales P/T Function
// rando(min/max) * cph

//P/T: storeRender 
// make a new tr
// .moop over the hours for the customers
// .make a new td for each of those

//func: rando(min, max)

//Create a store on the fly
//Event -- After a form is filled out ...
//Grab the data that was typed in
// new Store(name, min, max, cph);
// render just this store
//  RE-Render footer


//function: renderHeader
//clear the current thead
//create a new tr
//loop through array of times
//craete a th for each of them

//function: renderFooter
//   clear the current footer
//  create a new tr
// create a new td for each "hourly total" (could calulate in the foot function, or have another data struture that stores this)

//function: renderAllStore
// clear current tbody
// loops over the stores and call storeRender() for each store

//Initialize Step:
//  Create a store
//      [n] -- new Store(name, min, max, cph);
//  render header
//  render all the stores
//  render the footer



